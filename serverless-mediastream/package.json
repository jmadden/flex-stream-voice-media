{
  "name": "serverless-mediastream",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "twilio": "^3",
    "twilio-flex-token-validator": "^1.5.6"
  },
  "devDependencies": {
    "twilio-run": "^2"
  },
  "engines": {
    "node": "12"
  }
}
